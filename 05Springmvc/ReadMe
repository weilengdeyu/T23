2019年4月23日
  1.Springmvc执行流程

   1.1 request----发送给---->DispatcherServlet(前端控制器/中央调度器)

   1.2

   1.3

   1.4

   1.5

   1.6

  2.第一个案例
  2.1 节点依赖引入
    <!--SpringWebMVC-->
          <dependency>
              <groupId>org.springframework</groupId>
              <artifactId>spring-webmvc</artifactId>
              <version>${spring.version}</version>
          </dependency>
   2.2

 3.植入模型

 4.视图解析器

 5.解决静态资源无法访问的三种方案

 6.BeanNameUrlHandlerMapping
   寻址路径：
     打开工程的External Library----------------输入webmvc ，点击org.springframwork.web.servlet.DispathcerServlet.properties
     看就行了。


 7.SimpleUrlHandlerMapping
  /

2019年4月24日09:38:13
1.Springmvc定位：请求流转框架
  架构位置：Spring框架的Springweb模块中的一个组件
2.model2的升级版 。M：Model  V：View  C：Controller
3.Springmvc前端控制器 ---是一个Servlet
 Struts2   -------->Filter




 8.Handler

 9.AbstractController

 10.MultiActionController

 重要：还东西
 1.Springmvc概论
 2.底层代码  HandlerMapping
 3.数据结构
 4.Handler
 11.PropertiesMethodNameResolver
 12.ParameterMethodNameResolver
 13.ModelAndView
 14.InternalResourceViewResolver
 15.BeanNameViewResolver
 16.XmlViewResolver

 17.注解式开发

 2019年4月26日08:35:57
1.HandlerMapping执行流程
2.HashMap和LinkedList探讨数据结构
3.注解开发

2019年4月29日
1.SpringMVC
  架构
自我介绍：
    姓名-----毕业院校-----我会SSM框架，并且会Oracle和Mysql、
    之前在XXXXXXXXX工程师职务》项目-------》ERP系统-----》
    还做过一个YYY项目，经理。
Springmvc执行流程
Mybatis怎么看？？？JDBC优点
Spring AOP 应用场景是什么

2.开发步骤
  2.1  引入依赖
       springwebmvc
  2.2  web.xml中配置前端控制器

  2.3  控制器
       FirstController implements Controller{
          public ModelAndView handleRequest(HttpServletRequest request,HttpServletResonse respnose){
             ModelAndView mv=new  ModelAndView();
             mv.addObject("key","value");
             mv.setViewName("逻辑视图名");
          }
       }
  2.4 applicationContext.xml
       和bean节点绑定 ，公开一个地址 id就是地址 ，id="/hello" class="FirstController"

  2.5  jsp页面

  2.6视图解析器 逻辑视图设置 前缀(prefix)和后置(suffix)的
     默认的InternalResourceViewResolver

  2.7 HandlerMapping 也可以手工配置
      BeanNameUrlJHandlerMapping

3.HandlerMapping执行流程

4.注解开发
  @Controller
  @RequestMapping("/doList")
  public String doList(){
      return "xxxx";
  }
5.请求参数问题
   5种类型
    HTTPServletRequest
    HTTPServletResponse
    HTttpSession
    Model  model
    请求数据
      get：url地址  ?A=B&C=D
      post:  form表单的方式
6.路径变量
   @PathVariable
     @RequestMapping("/doList/{name}/{age}")
     public String doList(@PathVariable("name")String name ,@PathVariable("age") int age){
         return "xxxx";
     }

  http://localhost:8080/doList/张三/20
  类型转换器
      字符串 int
      字符串 日期  2019/04/29
      自自定义类型转换器
7.自动装配
  7.1  包扫描器和注解结合
       注解驱动

       零散参数
          view:标签中name值
          Controller：方法的参数名称一致
       对象类型
            view: 对象的属性名

            Controller：方法的参数名称一致
        集合对象类型
            view: 集合[0].属性

            Controller：方法的参数名称一致

8.乱码解决
   post
   通过  过滤器  手段解决了乱码问题

 过滤器：组件：
 1.就是请求到达真正的处理器之前先经过他。


 2.过滤器是可以双向过滤的，不仅仅可以对请求过滤，也可以对响应过滤

 3.可以配置多个过滤器，过滤器链

 4.过滤器的使用目的：就是把不满足要求的，拦截到之后。、
  走我们定制好的流程，一般都是回到登录。
  满足要求的继续走请求管道，他可能放行到下一个过滤器或者是放行到处理器。


8.Ajax
  全称：Asynchronized（异步的）javascript and XML  异常的js和XMl  css
  目标：局部刷新

  $.ajax({
    type:"POST",
    url:"/first",
    success:function(data){

    }
  })


js json串 转成json 方案




作业：
1.jquery注册事件的几种方式？
  click
  bind
  live
  on
  delegate
  总结
2.明天找两位面试
      自我介绍
      专业技能+项目经验（嗖嗖移动和易买网）
      SSM框架

3.SSM整合
     异常处理
     类型转换器
     文件上传
     数据校验
     拦截器
4.SSM三大框架 150页石墨文档
5.Linux命令
  JDK
  Mysql
  tomcat

2019年5月5日10:52:19
1.Echarts

  POI：
    导出报表
  JXL
    导出报表

Echarts：
ECharts，缩写来自 Enterprise Charts，商业级数据图表，是【百度】的一个开源的数据可视化工具，
一个纯 Javascript 的图表库，能够在 PC 端和移动设备上流畅运行，兼容当前绝大部分浏览器
（IE6/7/8/9/10/11，chrome，firefox，Safari，Opera等），底层依赖轻量级的 Canvas 库 ZRender，
ECharts 提供直观，生动，可交互，可高度个性化定制的数据可视化图表。创新的拖拽重计算、数据视图、
值域漫游等特性大大增强了用户体验，赋予了用户对数据进行挖掘、整合的能力。

所有人：数据掌控 ，  图片  ，数字  。。。。。。。。。。
更容易冲击视觉神经。
doubbo阿里内部，公开了，各个公司可以去用，当当网！！！！


Echarts典型图：
    柱状图
    折线图
    饼图
操作：
  基于JQuery之上的一个高级库。
回顾一下后台的数据回送到前台
 1.后台
  fastmybatis  新名词


 2.前台






















